# File Name Structor Pro (v5)
- Next.js 14 (App Router) + Tailwind
- Client-only Firebase Auth (Google)
- OpenAI rename + key validator
- ErrorBoundary + app/error.jsx
- JSZip dynamic import

## Run
1) Copy `.env.local.example` to `.env.local` and fill keys.
2) `npm install`
3) `npm run dev`
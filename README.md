# File Name Structor Pro (v6.3 — Gemini + JSZip fixed)
- No ENV needed (user inputs API keys in UI)
- Providers: OpenAI, Gemini
- Fixed build error: added `jszip` dependency

## Run
1) `npm install`
2) `npm run dev`
3) Deploy to Vercel and set no envs.
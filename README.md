# File Name Structor Pro (v6 — no Firebase)
- No backend auth dependency. Email gate is client-only.
- Users input their own AI API keys in the UI. Nothing is stored on server.
- OpenAI validate endpoint uses provided key only.

## Run
1) `npm install`
2) `npm run dev`
3) Deploy to Vercel (no env required)